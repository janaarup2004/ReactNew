import React from "react";

const UserContext = React.createContext()

export default UserContext;

{/* <UserContext>
<login/>
<Card>
    <Data/>
</Card>
<UserContext/> */}  //through Usercontext we can pass data to all the components without passing it through props.